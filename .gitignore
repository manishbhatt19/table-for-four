# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env

# Tooling caches
.pytest_cache/

# Perks vector store (regenerable from fixtures/perks_seed.json)
src/table_for_four/mcp_servers/perks/.chroma_perks/

# Member profile store (per-user long-term memory; may contain personal data)
src/table_for_four/agent/.chroma_profiles/

# LangGraph dev server / Studio artifacts
.langgraph_api/
.langgraph/

# SQLite bookings ledger (runtime state)
src/table_for_four/mcp_servers/booking/backend/bookings.db

# Agentic harness: the shared config (.claude/settings.json, agents/, skills/) is
# committed on purpose — it's part of the project. Only personal overrides aren't.
.claude/settings.local.json
