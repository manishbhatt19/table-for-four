# Copy to .env and fill in. .env is gitignored.
# Leave GOOGLE_PLACES_API_KEY blank/unset to run the search server in offline
# fixture mode. Set it to enable live Google Places API (New) calls.
GOOGLE_PLACES_API_KEY=
