{
  "permissions": {
    "allow": [
      "Bash(uv run pytest*)",
      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git log*)"
    ],
    "deny": [
      "Read(./.env)",
      "Edit(./.env)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "uv run pytest -q > .git/tf4-precommit.log 2>&1 || echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blocked: the offline test suite is failing. See .git/tf4-precommit.log for the output. Fix the failures before committing, or ask the user whether to override.\"}}'",
            "timeout": 180,
            "statusMessage": "Running the offline suite before commit"
          }
        ]
      }
    ]
  }
}
